# Section 9: Greedy I - Local Choices And Exchange

Greedy algorithms are useful when a local decision can be justified globally.
This section focuses on the proof habit that makes that safe: exchange.

## Study Order

1. Read `lesson.qmd`.
2. Solve the local exercises in order:
   - `a_activity_selection`
   - `b_rescue_boats`
   - `c_deadline_schedule`
   - `d_workshop_badges` (blind practice)
3. Run the checker:

```bash
python3 sections/09_greedy_exchange/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/09_greedy_exchange/check.py
```

4. Read `editorial.qmd` after making a serious attempt.
5. Work through `PRACTICE.md`.
