#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: Sort weights and pair the lightest possible person with the heaviest.
    return 0;
}

