# B. Coprime Pairs

Given an array `a`, count pairs `i < j` such that `gcd(a[i], a[j]) = 1`.

## Input

```text
n
a1 a2 ... an
```

`1 <= n <= 200,000`, `1 <= a[i] <= 1,000,000`.

## Output

Print the number of coprime pairs.

## Sample

Input:

```text
5
2 3 4 9 10
```

Output:

```text
6
```
