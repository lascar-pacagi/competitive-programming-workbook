# Section 26: Tree DP Basics

This section teaches dynamic programming on rooted trees: parent/child order,
postorder traversal, include/exclude states, tree matching, and counting
colorings.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_festival_invite`
   - `b_road_pairing`
   - `c_tree_colorings`
   - `d_road_guards` (blind practice: use its README only)
3. Run the checker:

```bash
python3 sections/26_tree_dp_basics/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/26_tree_dp_basics/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.
