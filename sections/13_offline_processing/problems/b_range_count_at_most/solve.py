"""Student solution for B. Range Count At Most."""

import sys


def main() -> None:
    # TODO: Sort values and queries by threshold, add positions to a Fenwick tree, restore answer order.
    data = sys.stdin.buffer.read().split()
    if not data:
        return
    raise NotImplementedError("solve this exercise")


if __name__ == "__main__":
    main()

