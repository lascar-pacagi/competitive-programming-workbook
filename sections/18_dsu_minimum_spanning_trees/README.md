# Section 18: DSU And Minimum Spanning Trees

This section teaches disjoint set union and Kruskal's algorithm. The main
themes are maintaining components under edge additions and choosing edges for a
minimum spanning tree or forest.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_network_growth`
   - `b_minimum_network`
   - `c_cluster_split`
3. Run the checker:

```bash
python3 sections/18_dsu_minimum_spanning_trees/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/18_dsu_minimum_spanning_trees/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

