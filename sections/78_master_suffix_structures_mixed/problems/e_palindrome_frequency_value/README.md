# Palindrome Frequency Value

For each distinct palindromic substring `p` of `s`, define its value as `|p|` times its number of occurrences. Print the maximum value.

Input: one lowercase string `s`, `1 <= |s| <= 200000`.

Sample input
```text
ababa
```
Sample output
```text
6
```
