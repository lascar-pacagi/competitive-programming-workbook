# Section 38: Static Queries and Binary Lifting

This section contains the lesson, editorial, and local judge exercises for the
topic.

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_static_range_minimum`: A. Static Range Minimum
   - `b_static_range_gcd`: B. Static Range GCD
   - `c_kth_ancestor`: C. Kth Ancestor
   - `d_ancestor_minimum` (blind practice: attempt it before the editorial)
3. Run the checker:

```bash
CP_TARGET=solution python3 sections/38_static_queries_binary_lifting/check.py
```
