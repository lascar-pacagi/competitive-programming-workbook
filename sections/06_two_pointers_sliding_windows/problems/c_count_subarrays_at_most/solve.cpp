#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: For each right endpoint, count all valid starts in the window.
    return 0;
}

