# Section 19: Dijkstra And Weighted Modeling

This section teaches shortest paths when edges have nonnegative weights.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_weighted_route_queries`
   - `b_grid_toll_path`
   - `c_discount_route`
3. Run the checker:

```bash
python3 sections/19_dijkstra_weighted_modeling/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/19_dijkstra_weighted_modeling/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

