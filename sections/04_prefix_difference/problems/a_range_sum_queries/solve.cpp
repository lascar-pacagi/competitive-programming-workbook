#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: Build prefix sums and answer each inclusive 1-indexed query.
    return 0;
}

