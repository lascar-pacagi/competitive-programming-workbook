"""Student solution for B. Bracket Completion."""

import sys


def main() -> None:
    # TODO: Fill the earliest possible '?' with '(' quotas, then validate prefixes.
    data = sys.stdin.buffer.read().split()
    if not data:
        return
    raise NotImplementedError("solve this exercise")


if __name__ == "__main__":
    main()

