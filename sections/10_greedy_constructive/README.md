# Section 10: Greedy II - Invariants And Constructive Problems

This section studies greedy algorithms that build or repair an object while
preserving a feasibility invariant.

## Study Order

1. Read `lesson.qmd`.
2. Solve the local exercises:
   - `a_non_decreasing_repairs`
   - `b_bracket_completion`
   - `c_bounded_sum_sequence`
3. Run the checker:

```bash
python3 sections/10_greedy_constructive/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/10_greedy_constructive/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

