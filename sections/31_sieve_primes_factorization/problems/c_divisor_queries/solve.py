import sys


def main() -> None:
    # TODO: factor with SPF and multiply (exponent + 1).
    data = sys.stdin.buffer.read().split()
    if not data:
        return
    raise NotImplementedError("solve.py is for your solution")


if __name__ == "__main__":
    main()
