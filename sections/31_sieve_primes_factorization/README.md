# Section 31: Sieve, Primes, and Factorization

This section teaches preprocessing primes with a sieve, building prefix counts,
and using smallest prime factors to factor many numbers quickly.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_prime_counter`
   - `b_factor_signature`
   - `c_divisor_queries`
   - `d_square_completion` (blind practice: use its README only)
3. Run the checker:

```bash
python3 sections/31_sieve_primes_factorization/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/31_sieve_primes_factorization/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.
