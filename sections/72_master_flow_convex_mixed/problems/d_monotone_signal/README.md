# D. Monotone Signal

Replace the integer sequence `a` by any nondecreasing integer sequence `b`. Minimize `sum |a[i]-b[i]|` and print the minimum cost.

## Input
`n`, followed by `n` integers.

`1 <= n <= 200000`, `|a[i]| <= 10^9`.

## Sample
```text
5
5 1 4 2 7
```
```text
6
```
