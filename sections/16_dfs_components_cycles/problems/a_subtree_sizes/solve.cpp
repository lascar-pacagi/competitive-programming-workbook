#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: root the tree at 1 and compute subtree sizes with DFS.
    return 0;
}

