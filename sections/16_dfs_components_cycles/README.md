# Section 16: DFS, Components, And Cycles

This section teaches depth-first search as a traversal pattern for trees and
graphs. The focus is on DFS state, parent relationships, postorder work, and
cycle detection.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_subtree_sizes`
   - `b_undirected_cycle`
   - `c_directed_cycle`
3. Run the checker:

```bash
python3 sections/16_dfs_components_cycles/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/16_dfs_components_cycles/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

