#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: Use prefix sums and a deque with increasing prefix values.
    return 0;
}

