# Section 12: Stacks, Queues, And Deques

This section studies data structures that keep only the useful candidates:
stacks for nearest-element problems, queues for first-in-first-out simulation,
and deques for sliding-window and prefix-sum optimization.

## Study Order

1. Read `lesson.qmd`.
2. Solve the local exercises:
   - `a_next_greater`
   - `b_sliding_window_max`
   - `c_shortest_subarray_at_least`
   - `d_bounded_spread_subarrays` (blind practice)
3. Run the checker:

```bash
python3 sections/12_stacks_queues_deques/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/12_stacks_queues_deques/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.
