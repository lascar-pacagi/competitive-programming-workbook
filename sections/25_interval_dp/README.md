# Section 25: Interval DP

This section teaches dynamic programming on contiguous ranges: filling by
length, splitting intervals, and choosing the first or last operation inside a
range.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_merge_piles`
   - `b_palindrome_repairs`
   - `c_treasure_balloons`
3. Run the checker:

```bash
python3 sections/25_interval_dp/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/25_interval_dp/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

