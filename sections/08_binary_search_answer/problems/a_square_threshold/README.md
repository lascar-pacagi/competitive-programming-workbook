# A. Square Threshold

## Statement

For each query, given a nonnegative integer `n`, print the smallest
nonnegative integer `x` such that:

```text
x * x >= n
```

## Input

```text
T
n1
n2
...
nT
```

`1 <= T <= 200000` and `0 <= n <= 10^18`.

## Output

For each query, print the required `x`.

