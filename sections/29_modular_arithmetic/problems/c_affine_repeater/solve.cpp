#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: use a^k and the modular geometric sum 1 + a + ... + a^(k-1).
    return 0;
}
