# Section 22: One-Dimensional DP

This section starts Dynamic Programming I. The goal is to turn a linear
sequence into states, transitions, base cases, and a safe computation order.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_broken_stairs`
   - `b_training_score`
   - `c_energy_route`
3. Run the checker:

```bash
python3 sections/22_one_dimensional_dp/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/22_one_dimensional_dp/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.

