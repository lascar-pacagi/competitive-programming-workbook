# Digit Sum Range

For each query `[L,R,S]`, count integers `x` with `L <= x <= R` whose decimal digit sum is exactly `S`. `0 <= L <= R <= 10^18`, `0 <= S <= 171`, and `q <= 10000`. Count zero with digit sum zero.

Sample input
```text
2
0 20 2
10 15 6
```
Sample output
```text
3
1
```
