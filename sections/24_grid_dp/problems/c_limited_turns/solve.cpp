#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // TODO: dp[r][c][turns][dir] counts paths ending at a cell with last move dir.
    return 0;
}

