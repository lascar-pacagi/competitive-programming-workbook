# Series Inverse

For polynomial `A(x)` with `A[0] != 0`, print the first `n` coefficients of the unique formal series `B` satisfying `A*B = 1 (mod x^n)`. Modulus is `998244353`; `n <= 200000`.

Sample input
```text
4
1 2 0 0
```
Sample output
```text
1 998244351 4 998244345
```
