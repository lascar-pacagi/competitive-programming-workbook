# Quadratic Residue Archive

An odd prime `p < 2^63` is fixed. For each query value `a`, print the smaller modular square root of `a`, or `-1` if none exists. `q <= 100000`.

Sample input
```text
7 3
2
3
4
```
Sample output
```text
3
-1
2
```
