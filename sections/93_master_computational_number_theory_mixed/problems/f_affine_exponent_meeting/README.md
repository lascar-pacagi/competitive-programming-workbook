# Affine Exponent Meeting

For prime `p <= 10^12`, find the smallest `t >= 0` such that `a^(u*t+v) = b (mod p)`, where `a,b` are nonzero and `u,v >= 0`, or print `-1`. At most 50 queries.

Sample input
```text
2
13 2 8 1 0
13 2 8 2 0
```
Sample output
```text
3
-1
```
