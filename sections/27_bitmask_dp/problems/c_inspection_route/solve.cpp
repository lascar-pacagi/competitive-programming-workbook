#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: dp[mask][last] = min cost to start at 0, visit mask, and end at last.
    return 0;
}
