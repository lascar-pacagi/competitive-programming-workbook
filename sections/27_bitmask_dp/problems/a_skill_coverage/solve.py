import sys


def main() -> None:
    # TODO: dp[mask] = minimum cost to cover exactly the skills in mask.
    data = sys.stdin.buffer.read().split()
    if not data:
        return
    raise NotImplementedError("solve.py is for your solution")


if __name__ == "__main__":
    main()
