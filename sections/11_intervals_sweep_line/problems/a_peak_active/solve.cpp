#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // TODO: Sweep events. For half-open intervals, process ends before starts at the same coordinate.
    return 0;
}

