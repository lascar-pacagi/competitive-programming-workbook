# Checksum Digit Sum

For each query, count integers `x` in `[L,R]` whose decimal digit sum is exactly `S` and whose remainder modulo `M` is zero. `0 <= L <= R <= 10^18`, `0 <= S <= 171`, `1 <= M <= 50`, and `q <= 1000`.

Sample input
```text
2
0 30 3 3
10 20 2 2
```
Sample output
```text
4
1
```
