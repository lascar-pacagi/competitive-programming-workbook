# Section 23: Knapsack And Subset DP

This section teaches capacity-based dynamic programming: 0/1 knapsack,
complete knapsack, and subset-sum feasibility with state compression.

## Study Order

1. Read `lesson.qmd`.
2. Attempt the local exercises:
   - `a_budget_selection`
   - `b_unlimited_training`
   - `c_possible_sums`
   - `d_balanced_split` (blind practice: use its README only)
3. Run the checker:

```bash
python3 sections/23_knapsack_subset_dp/check.py
```

To test the reference solutions:

```bash
CP_TARGET=solution python3 sections/23_knapsack_subset_dp/check.py
```

4. Read `editorial.qmd`.
5. Work through `PRACTICE.md`.
