# Appendix A: Lazy Propagation

Study this appendix immediately after Section 37. It is a standalone lesson on
the range-update structure that was previously missing from the published map.
Use the three exercises in the lesson, starting with Section 37's existing
`c_lazy_range_add_sum` implementation drill.

Render the sources when building this appendix directly:

```bash
quarto render appendices/lazy_propagation/lesson.qmd --to pdf
quarto render appendices/lazy_propagation/editorial.qmd --to pdf
```
